# Dependencias: se reinstalan con `npm install`.
# motion.js es una copia propia dentro del repo, esa sí se versiona.
node_modules/

# Caché del hook de diseño, es local de cada máquina
.impeccable/

# macOS
.DS_Store

# Credenciales de sync/sync.mjs (Mailchimp, EmailOctopus) — nunca se commitea.
.env

# Salida de sync/sync.mjs: datos reales de campañas, no hace falta versionarlo.
# Anclado a la raíz: api/metricas.js es código (la función de Vercel), no esto.
/metricas.js
